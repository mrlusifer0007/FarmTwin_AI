# URL of the deployed backend. Locally this defaults to http://localhost:8000
# if unset (see src/services/api.js), so this file only matters in production.
VITE_API_BASE=https://your-backend.onrender.com
